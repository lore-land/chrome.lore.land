import './stylesheets/content.scss';
import { initRuntimeListener } from "./helpers/chrome/initRuntimeListener.mjs";

// Initialize the runtime listener for Chrome extension events
initRuntimeListener();
